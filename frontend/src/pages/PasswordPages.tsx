import React, { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { sendPasswordReset, updatePassword } from '../utils/authSession';

const Shell: React.FC<React.PropsWithChildren> = ({children}) => <main className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50 px-4 py-10"><section className="mx-auto mt-24 max-w-md rounded-3xl border border-slate-200 bg-white p-8 shadow-xl">{children}</section></main>;
export const ForgotPasswordPage = () => { const [email,setEmail]=useState(''); const [message,setMessage]=useState(''); return <Shell><h1 className="text-3xl font-bold">Forgot Password</h1><p className="mt-2 text-gray-600">We’ll email you a secure reset link.</p><form className="mt-6 space-y-4" onSubmit={async(e)=>{e.preventDefault();try{await sendPasswordReset(email);setMessage('Check your email for the reset link.');}catch(error){setMessage(error instanceof Error?error.message:'Reset failed.');}}}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3" placeholder="you@example.com"/><button className="w-full rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 px-4 py-3 font-semibold text-white">Send Reset Link</button>{message&&<p className="text-sm text-blue-700">{message}</p>}</form><Link to="/login" className="mt-5 inline-flex text-sm font-semibold text-blue-700">Back to login</Link></Shell>; };
export const ResetPasswordPage = () => { const navigate=useNavigate(); const [password,setPassword]=useState(''); const [error,setError]=useState(''); return <Shell><h1 className="text-3xl font-bold">Reset Password</h1><form className="mt-6 space-y-4" onSubmit={async(e)=>{e.preventDefault();try{await updatePassword(password);navigate('/dashboard');}catch(reason){setError(reason instanceof Error?reason.message:'Reset failed.');}}}><input type="password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3" placeholder="New password"/><button className="w-full rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 px-4 py-3 font-semibold text-white">Update Password</button>{error&&<p className="text-sm text-red-600">{error}</p>}</form></Shell>; };
