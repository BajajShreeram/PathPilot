import { useMemo, useState } from 'react';
import { ProofUploadField, type ProofMetadata } from '../components/ui/ProofUploadField';
import { ACHIEVEMENT_CATEGORIES, ACHIEVEMENT_LEVELS, getAchievements, saveAchievements, type Achievement } from '../utils/achievementsStorage';

const emptyAchievement = (): Achievement => ({ id: '', title: '', category: 'Projects', organization: '', date: '', description: '', level: 'School', result: '', skillsGained: [], proofLink: '', proofFileName: null, proofFileType: null, proofFileSize: null, proofPreviewUrl: null, proofStorageUrl: null });
const proof = (item: Achievement): ProofMetadata => ({ proofFileName: item.proofFileName || null, proofFileType: item.proofFileType || null, proofFileSize: item.proofFileSize || null, proofPreviewUrl: item.proofPreviewUrl || null, proofStorageUrl: item.proofStorageUrl || null });

export const AchievementsPage = () => {
  const [achievements, setAchievements] = useState<Achievement[]>(getAchievements); const [filter, setFilter] = useState('All'); const [editing, setEditing] = useState<Achievement | null>(null); const [viewing, setViewing] = useState<Achievement | null>(null);
  const visible = useMemo(() => achievements.filter((item) => filter === 'All' || item.category === filter).sort((a, b) => b.date.localeCompare(a.date)), [achievements, filter]);
  const persist = (items: Achievement[]) => { setAchievements(items); saveAchievements(items); };
  const submit = (event: React.FormEvent) => { event.preventDefault(); if (!editing) return; const item = { ...editing, id: editing.id || crypto.randomUUID() }; persist(editing.id ? achievements.map((current) => current.id === item.id ? item : current) : [...achievements, item]); setEditing(null); };
  const remove = (item: Achievement) => { if (window.confirm(`Delete “${item.title}”?`)) persist(achievements.filter((current) => current.id !== item.id)); };
  return <div className="min-h-screen bg-gradient-to-br from-gray-50 to-blue-50/30 px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl"><div className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-widest text-blue-600">Build your profile</p><h1 className="mt-2 text-4xl font-bold text-gray-900">Achievements</h1><p className="mt-2 text-gray-600">Add experiences that strengthen your PathPilot recommendations.</p></div><button onClick={() => setEditing(emptyAchievement())} className="rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 px-6 py-3 font-semibold text-white shadow-lg">Add Achievement</button></div><div className="mb-7 flex flex-wrap gap-2 rounded-2xl border border-gray-100 bg-white p-4 shadow-sm">{['All', ...ACHIEVEMENT_CATEGORIES].map((category) => <button key={category} onClick={() => setFilter(category)} className={`rounded-full px-4 py-2 text-sm font-semibold ${filter === category ? 'bg-blue-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'}`}>{category}</button>)}</div>{visible.length ? <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{visible.map((item) => <article key={item.id} className="flex min-w-0 flex-col rounded-2xl border border-gray-100 bg-white p-6 shadow-lg"><div className="flex justify-between gap-3"><span className="rounded-full bg-purple-100 px-3 py-1 text-xs font-bold text-purple-700">{item.category}</span><span className="text-xs font-semibold text-gray-500">{item.level}</span></div><h2 className="mt-4 break-words text-xl font-bold text-gray-900">{item.title}</h2><p className="mt-1 truncate text-sm text-gray-500">{item.organization || 'Independent'} · {item.date || 'Date not set'}</p><p className="mt-4 line-clamp-3 text-sm text-gray-700">{item.description || item.result || 'No description added.'}</p><div className="mt-6 flex flex-wrap gap-2"><button onClick={() => setViewing(item)} className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">View Details</button><button onClick={() => setEditing({ ...item })} className="rounded-lg bg-gray-100 px-4 py-2 text-sm font-semibold text-gray-700">Edit</button><button onClick={() => remove(item)} className="rounded-lg px-3 py-2 text-sm font-semibold text-red-600 hover:bg-red-50">Delete</button></div></article>)}</div> : <div className="rounded-3xl border-2 border-dashed border-gray-200 bg-white py-20 text-center"><div className="text-6xl">🏆</div><h2 className="mt-5 text-2xl font-bold text-gray-900">No achievements yet</h2><p className="mx-auto mt-2 max-w-md text-gray-600">Add your first project, certificate, competition, or experience.</p><button onClick={() => setEditing(emptyAchievement())} className="mt-6 rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 px-6 py-3 font-semibold text-white">Add Your First Achievement</button></div>}</div>{editing && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4"><form onSubmit={submit} className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl sm:p-8"><div className="flex items-center justify-between"><h2 className="text-2xl font-bold text-gray-900">{editing.id ? 'Edit Achievement' : 'Add Achievement'}</h2><button type="button" onClick={() => setEditing(null)} className="text-2xl text-gray-500">×</button></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className="sm:col-span-2"><span className="mb-2 block text-sm font-semibold">Title</span><input required value={editing.title} onChange={(event) => setEditing({ ...editing, title: event.target.value })} className="w-full rounded-xl border border-gray-300 px-4 py-3" /></label><label><span className="mb-2 block text-sm font-semibold">Category</span><select value={editing.category} onChange={(event) => setEditing({ ...editing, category: event.target.value as Achievement['category'] })} className="w-full rounded-xl border border-gray-300 px-4 py-3">{ACHIEVEMENT_CATEGORIES.map((item) => <option key={item}>{item}</option>)}</select></label><label><span className="mb-2 block text-sm font-semibold">Level</span><select value={editing.level} onChange={(event) => setEditing({ ...editing, level: event.target.value as Achievement['level'] })} className="w-full rounded-xl border border-gray-300 px-4 py-3">{ACHIEVEMENT_LEVELS.map((item) => <option key={item}>{item}</option>)}</select></label><label><span className="mb-2 block text-sm font-semibold">Organization</span><input value={editing.organization} onChange={(event) => setEditing({ ...editing, organization: event.target.value })} className="w-full rounded-xl border border-gray-300 px-4 py-3" /></label><label><span className="mb-2 block text-sm font-semibold">Date</span><input type="date" value={editing.date} onChange={(event) => setEditing({ ...editing, date: event.target.value })} className="w-full rounded-xl border border-gray-300 px-4 py-3" /></label><label className="sm:col-span-2"><span className="mb-2 block text-sm font-semibold">Description</span><textarea rows={3} value={editing.description} onChange={(event) => setEditing({ ...editing, description: event.target.value })} className="w-full rounded-xl border border-gray-300 px-4 py-3" /></label><label><span className="mb-2 block text-sm font-semibold">Result or Award</span><input value={editing.result} onChange={(event) => setEditing({ ...editing, result: event.target.value })} className="w-full rounded-xl border border-gray-300 px-4 py-3" /></label><label><span className="mb-2 block text-sm font-semibold">Skills Gained</span><input value={editing.skillsGained.join(', ')} onChange={(event) => setEditing({ ...editing, skillsGained: event.target.value.split(',').map((item) => item.trim()).filter(Boolean) })} className="w-full rounded-xl border border-gray-300 px-4 py-3" /></label><label className="sm:col-span-2"><span className="mb-2 block text-sm font-semibold">Certificate or Proof Link (optional)</span><input type="url" value={editing.proofLink} onChange={(event) => setEditing({ ...editing, proofLink: event.target.value })} className="w-full rounded-xl border border-gray-300 px-4 py-3" /></label><div className="sm:col-span-2"><span className="mb-2 block text-sm font-semibold">Proof File (optional)</span><ProofUploadField value={proof(editing)} onChange={(metadata) => setEditing({ ...editing, ...metadata })} /></div></div><div className="mt-7 flex justify-end gap-3"><button type="button" onClick={() => setEditing(null)} className="rounded-xl px-5 py-3 font-semibold text-gray-600">Cancel</button><button className="rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 px-6 py-3 font-semibold text-white">Save Achievement</button></div></form></div>}{viewing && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/50 p-4" onClick={() => setViewing(null)}><div className="max-h-[92vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white p-7 shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex justify-between gap-4"><div><span className="text-sm font-semibold text-purple-700">{viewing.category} · {viewing.level}</span><h2 className="mt-2 text-3xl font-bold text-gray-900">{viewing.title}</h2></div><button onClick={() => setViewing(null)} className="text-2xl text-gray-500">×</button></div><div className="mt-6 space-y-4 text-gray-700"><p><strong>Organization:</strong> {viewing.organization || 'Independent'}</p><p><strong>Date:</strong> {viewing.date || 'Not set'}</p><p><strong>Description:</strong> {viewing.description || 'Not provided'}</p><p><strong>Result:</strong> {viewing.result || 'Not provided'}</p><p><strong>Skills:</strong> {viewing.skillsGained.join(', ') || 'Not provided'}</p>{viewing.proofFileName && <div className="rounded-xl border border-gray-200 bg-gray-50 p-4">{viewing.proofFileType?.startsWith('image/') && viewing.proofPreviewUrl ? <img src={viewing.proofPreviewUrl} alt={`${viewing.title} proof`} className="max-h-64 w-full rounded-lg object-contain" /> : <p className="font-semibold">📄 {viewing.proofFileName}</p>}<p className="mt-2 text-sm text-gray-500">Temporary local preview only. Upload storage can be connected later.</p></div>}{viewing.proofLink && <a href={viewing.proofLink} target="_blank" rel="noreferrer" className="font-semibold text-blue-700">View certificate or proof ↗</a>}</div></div></div>}</div>;
};
